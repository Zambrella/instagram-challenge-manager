class InstagramLoginRepository {
  const InstagramLoginRepository();

  /// Using the [code] provided by the Instagram API,
  /// send it to the server and log the user in.
  Future<void> loginWithCode(String code) async {}
}
